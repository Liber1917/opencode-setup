interface:
  display_name: "Opencode Setup"
  short_description: "Repo-specific patterns and workflows for opencode-setup"
  default_prompt: "Use the opencode-setup repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true